# java-proj
java security vulnerabilities

## Database credentials

Set these environment variables before running or deploying the application:

- `DB_USERNAME`
- `DB_PASSWORD`

Example:

```bash
export DB_USERNAME=your_db_user
export DB_PASSWORD=your_db_password
mvn test -B
```
